import { shallowMount } from '@vue/test-utils';
import Permissions from '../permissions/Permissions.vue';
describe('Permissions', () => {
    it('works', () => {
        const wrapper = shallowMount(Permissions);
    });
});